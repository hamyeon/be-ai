# Vintic AI 시스템 기술 문서

크롤링부터 Vision 분석, 가격 계산, 추천까지 — AI 트랙의 전체 기술을 한 곳에 모은
살아있는 문서다. 새 측정이나 구조 변화가 생기면 여기에 추가한다.
각 주제의 깊은 내용은 문서 지도(맨 아래)의 개별 문서에 있다.

**관통하는 원칙 하나**: 모든 숫자는 근거가 있어야 하고, 근거가 없으면 없다고
말한다. 측정하지 않은 값을 측정한 것처럼 내보내지 않는다.

## 갱신 로그

| 날짜 | 내용 |
| --- | --- |
| 2026-09-04 | 최초 작성. 색상 계열 시세(#93)까지 반영 |
| 2026-09-06 | C등급 키워드 정밀화: 부정어 확장·정도 한정 처리로 C 역전 해소(0.32→0.23) |
| 2026-09-07 | KREAM 우선순위 17개 키워드 수집 완주. 참조 26개 모델, 계수 표본 1,884건. 척70(당근 443건, 감가 큰 모델) 편입으로 공통 계수 하향 - 풀 구성 변화의 정직한 반영이며 모델별 행이 있는 모델은 영향 없음 |

---

## 0. 한눈에 보는 그림

```
[사용자]                        [데이터 파이프라인 (오프라인)]
사진 업로드                      당근·후르츠·KREAM 크롤링
  │                                │
  ▼                                ▼
Vision 3단계 분석            정제·캘리브레이션 (별칭·필터·정규화)
(브랜드/모델/색상/사이즈/상태)       │
  │                                ▼
  ▼                          시세 CSV 4종
사용자 확인·수정  ─────────►  ┌ used_market_prices        모델 시세 45개
  │                          ├ used_market_prices_by_color 색상 버킷 82개
  ▼                          ├ condition_rates            상태 계수(실측)
가격 계산 ◄────────────────  └ kream_color_premiums       색상 프리미엄
  │
  ▼
상품 등록 → 임베딩 벡터 생성 → 개인화 추천
```

---

## 1. 데이터 수집 (크롤링)

### 1-1. 당근마켓 — 시세의 몸통

- **왜 당근인가**: 우리 서비스가 개인 간 중고 거래라, 예측해야 하는 가격은
  KREAM 검수 새제품가가 아니라 당근에서 손바뀜되는 가격에 가깝다.
  실측으로도 당근의 새상품(DS) 매물조차 KREAM 체결가의 70% 수준에 올라온다.
- **수집 방식**: robots.txt가 허용하는 검색 경로만, 요청 간격 1.5초.
  서울 22개 동(20대 밀집·패션 상권 위주) × 검색어 36개(브랜드 19 + 일반 8 +
  표본 얇은 모델 직접 검색 9).
- **누적 방식**: URL 기준 중복 제거 후 이어붙인다. 주기적으로 재실행하면
  새 매물만 쌓인다(적정 주기: 하루 1회 — 시간 단위 재실행은 신규가 거의 없음을 실측).
- **현재 규모**: 원본 약 47,000건.

### 1-2. 후르츠패밀리 — 당근과 같은 종류의 가격

개인 간 중고 거래라 당근과 한 바구니에 합쳐 시세를 낸다. 특정 모델
(닥터마틴 1461, 헌터 레인부츠, XT-6)에서 비중이 크다.

### 1-3. KREAM — 시세의 잣대

- **역할 셋**: ① 상태 계수의 분모(새제품가 대비 감가율) ② 2순위 가격의 기준 시세
  ③ 색상 프리미엄(상품 = 컬러웨이 구조라 색상별 체결가가 원래 형태).
- **제약**: 소프트 차단(짧은 버스트 ~15요청만 허용, 이후 전부 500). 대응:
  요청 간격 10~20초, 연속 5회 실패 시 자동 중단, 우선순위 모델 검색어부터 소비.
  탐지 우회는 하지 않는다 — 차단되면 시간을 두고 다시 간다.
- **수집 단위**: 상품 정보 + 최근 체결 5건(사이즈/가격/시각). 참조로 쓸 상품은
  사람이 고른다(표준 컬러웨이만 — 아래 3-3).

### 1-4. eBay — 참고 정보로 강등 (측정으로 결정)

당근 실거래 예측 오차를 쟀더니 KREAM 단독 25% < 혼합(0.7/0.3) 38% < eBay 단독
155%. eBay는 해외 호가라 국내 실거래의 약 2.5배이고, 중고 매물 호가인데
새제품가처럼 상태계수를 곱하는 이중 감가 버그도 있었다. 기준 시세에서 빼고
응답에 참고로만 싣는다. (`secondary_price_eval.py`)

---

## 2. 데이터 정제·캘리브레이션

크롤링 원본은 그대로 못 쓴다. 다섯 겹의 정제를 거친다. (`crawler/calibration/`)

| 단계 | 도구 | 왜 필요한가 (전부 실측에서 배운 것) |
| --- | --- | --- |
| 한글→모델 매핑 | `model_aliases.py`, MODELS 목록 | "에어포스1"과 "Air Force 1 Low"는 문자로 안 겹친다. 숫자 별칭(530, 1461)은 브랜드 힌트가 함께 있어야 인정 — "530"만 잡으면 무관한 숫자가 붙는다 |
| 오염 매물 필터 | `listing_filters.py` | 협업·한정판(가격 2.4배), 묶음판매, 아동용(가격 1/10)이 섞이면 시세가 왜곡된다 |
| 참조 품질 분류 | `reference_quality.py` | KREAM 참조가 한정 컬러면 비율이 엉터리가 된다(에어맥스95 Neon 참조 → 계수 0.14 사건). 표준 컬러웨이 참조만 계수에 쓴다 |
| 상태 재분류 | `fine_condition.py` | 매물 설명("3번 착화" → A, "사용감" → B)으로 등급을 붙인다. 부정 표현("하자 없어요") 처리 필수 |
| 색상 계열 정규화 | `color_families.py` | gray/grey/회색/차콜은 같은 색이다. 계열로 접지 않으면 표기 차이로 매칭이 깨진다. 백엔드 `ColorFamilies.java`와 같은 표를 쓴다(어긋나면 시세가 영영 매칭 안 됨) |

---

## 3. Vision 분석 (사진 → 상품 정보)

### 구조: 3단계 분리 호출 (gpt-4o)

1. **실루엣**(저해상도): 신발 종류, 브랜드, 모델, 색상
2. **라벨 판독**(고해상도): 사이즈, 스타일 코드
3. **상태 판정**(고해상도): 등급(DS/A/B/C/UNKNOWN), 하자 목록, 상태 설명, 박스 여부

환각 방지 장치: Structured Outputs(json_schema+strict), 근거(evidence) 없는
필드는 검증기가 null 처리, 공식 컬러웨이명은 라벨에 없으면 금지, 모르면
UNKNOWN/null(기권이 오답보다 낫다 — 기권은 사용자 확인으로 메워진다).

### 실측 성능 (평가 하네스, 실제 매물 49케이스)

| 항목 | 수치 | 비고 |
| --- | --- | --- |
| JSON 준수율 | 100% | 회귀 기준선 |
| 브랜드 | 89~94% | |
| 모델명 | 근사 100% (정확 표기 22%) | 시세 매칭은 근사로 충분 |
| 사이즈 | 라벨 사진 없으면 0% | 구조적 한계, 사용자 수정으로 커버 |
| 상태 등급 | 근사 포함 87% | 완전 오답 0 — 틀려도 한 등급 차이 |
| 색상 | 79% (오답은 전부 저채도·어두운 경계색) | #90에서 채점 체계 신설 |
| 케이스당 지연 | 평균 12.6초 | 단계별 3.2/2.4/9.3초 |

Vision 결과는 전부 **초안**이다 — 사용자 확인 화면에서 수정 가능하고,
최종 확정값으로 가격이 계산된다.

---

## 4. 가격 계산

### 4-1. 전체 폴백 체인

```
요청: (브랜드, 모델, 색상, 등급, 구성품)

1순위: 중고 실거래 (당근+후르츠, 45개 모델·5,742건)
  ①-a (모델, 색상 계열) 버킷 있음 (82개, 표본 10건+)
       → 그 색 실거래 중앙값 기준
  ①-b 없으면: KREAM 색상 프리미엄 있음
       → 모델 중앙값 × (그 색의 KREAM 체결가 / 모델 평균)
  ①-c 없으면: 모델 전체 중앙값 기준
  공통: × 상태 비율(그 등급 계수 ÷ 전체 매물 계수) × 구성품 반영률
  권장 범위 = 실거래 IQR(25~75% 구간) — ±5% 같은 임의 폭이 아니라 실제 분포

2순위: 1순위 매칭이 없으면 KREAM 새제품가 × 상태 계수 × 구성품
       (eBay는 참고 표시만)

3순위: 그것도 없으면 "시세 정보 없음" + 요청 로깅
       (틀린 값보다 없음이 정직하다. 로그는 다음 커버리지 확대의 우선순위 근거)
```

어느 경로로 계산했는지, 계수가 실측인지 기본값인지, 표본이 몇 건인지 —
전부 `reason` 문구로 응답마다 공개한다.

### 4-2. 상태 계수 (실측, 2026-09-07 기준)

계수 = 당근 실거래 중앙값 ÷ KREAM 표준 컬러웨이 새제품가. 원본은
`condition_rates.csv`이고 재측정 때마다 갱신된다.

| 등급 | 계수 | 출처 |
| --- | --- | --- |
| DS(새상품) | 0.612 | 실측 387건 |
| S(거의 새것) | 0.404 | 실측 105건 — 기본값 0.70이 크게 후했음이 드러남 |
| A | 0.369 | 실측 628건 |
| B | 0.266 | 실측 300건 |
| C | 0.20 | 기본값 — 과포착 정제 후 실측 0.23(28건)으로 서열 정합, 표본 부족으로 채택 보류 |
| ALL(기준선) | 0.389 | 실측 1,884건 — 1순위 비율의 분모 |

- 분모가 ALL(전체 매물)인 이유: 시세 중앙값이 전체 매물에서 나온 값이라
  분자·분모의 모집단이 같아야 한다. UNKNOWN을 분모로 쓰면 새상품이 빠져
  전체가 일괄 높게 추천되는 편향이 생긴다(실측으로 확인 후 교정).
- 상태 미상(UNKNOWN) 요청은 비율 1.0 — 시장 중앙값 그대로.
- 모델별 계수가 있으면 우선(감가 속도가 모델마다 다르다 — 에어포스1 0.44 vs
  993 0.61. 통합 계수로 뭉개면 993이 31% 낮게 추천된다).

### 4-3. 색상 반영 (#93)

- 색상별 가격차는 실재한다: 슈퍼스타 블랙 -39% vs 화이트 -5%, 덩크로우 블루 +112%.
- 색상 표기는 계열로 정규화해 비교한다(gray=grey=회색). 색상 버킷은 표본
  10건+일 때만 존재 — **색상 시세는 통계적으로 설 때만 쓰고, 정확도가
  나빠지는 경로는 없다.**
- 희소성(매물 수)은 반영하지 않는다: 점유율↔프리미엄 상관 +0.108(무상관).
  희소한 색에는 한정판과 비인기색이 섞여 매물 수로는 구분 불가(ADR 12).

---

## 5. 검증 체계 — 어떻게 믿을 수 있는 숫자로 유지하는가

### 5-1. 측정 (전부 재현 가능한 스크립트)

| 측정 | 결과 | 도구 |
| --- | --- | --- |
| Vision 필드별 정확도·JSON 준수율 | §3 표 | 평가 하네스 (실제 API 호출) |
| 2순위 기준 시세 예측력 | KREAM 단독 25% 승 | `secondary_price_eval.py` |
| 희소성↔가격 상관 | +0.108, 반영 안 함 | `rarity_correlation.py` |
| 색상 버킷 상태 구성 편향 | 상관 +0.218, 극단 버킷 가드 | `pricing-agent.md` §3-3 |
| 개인화 추천 오버헤드 | +13/+98/+288ms @100/1k/3k | 코드 주석·ADR 2 |

### 5-2. 자동 가드 (재산출 때마다 잘못된 값을 걸러낸다)

| 가드 | 규칙 | 잡아낸 실제 사례 |
| --- | --- | --- |
| 표본 기준 | 공통 50건+/모델별 10건+/색상 10건+ 미만이면 기본값·미산출 | S 12건 시절 교체 보류 |
| 등급 서열(공통·모델별) | DS≥S≥A≥B≥C 깨지면 그 등급 폐기 | 슈퍼스타 B>DS, 574 A<B, 공통 C>B |
| 상태 구성 편중 | 색상 버킷 새상품 비중이 모델 대비 30%p 초과 이탈 시 제외 | 올드스쿨 그레이(DS 60% vs 18%) |
| 프리미엄 범위 | [0.5, 2.0] 밖은 폐기(색이 아니라 별개 상품 신호) | — |
| 성별 교란 | KREAM (W) 상품은 프리미엄 계산 제외 | 코르테즈 W 0.73 왜곡 |
| 참조 사이즈 | 사이즈 정상 범위(150~350) 밖 참조 폐기 | Samba 아동 사이즈 사건 |

### 5-3. 테스트

- 단위: 계수 조합 방식, 폴백 체인, 색상 정규화(단어 단위·긴 표기 우선),
  서열 유지, 근거 공개 — 값이 아니라 **성질**을 검증해 재측정에도 안 깨진다.
- E2E 21케이스: AI 트랙 14(색상 경로 포함) + 전 구간(등록→결제) 7.
  커밋 시점 결함을 잡기 위해 @Transactional 없이 실제 MySQL에 커밋한다.

### 5-4. 아직 못 잰 것 (다음 채점표)

- **최종 정확도**: "추천가대로 올리면 실제로 그 가격에 팔리는가"는 정답지(실제
  낙찰가)가 없어 못 쟀다. 서비스가 열리면 "추천가 대비 낙찰가"가 진짜 채점표다.
- 색상 미판독 매물 43%: Vision 사진 라벨링(gpt-4o-mini, 시세 매물 전체 ~$3)으로
  채울 계획 — 제목 색상이 있는 절반으로 라벨 신뢰도를 선검증한 뒤 쓴다.
- 당근 가격은 호가지 체결가가 아니다. IQR을 함께 내려 분포를 숨기지 않는다.

---

## 6. 추천 엔진 (요약)

- 상품 등록 시 text-embedding-3-small(1536차원)으로 벡터 생성. 실패해도 상품
  등록은 롤백되지 않는다(트랜잭션 분리 — troubleshooting 2번의 교훈).
- 개인화: 행동 로그 가중(입찰 3.0/찜 2.0/조회 1.0/체류 1.0)으로 취향 벡터를
  만들어 코사인 유사도 정렬. 행동 3건 미만은 Cold Start Fallback(마감임박+인기).
- 벡터 없는 상품도 버리지 않고 뒤에 붙인다(similarity는 정직하게 null).
- Fallback은 Redis 60초 캐시 + 입찰/경매 종료 시 즉시 무효화.

## 7. 관측성 (요약)

- 모든 OpenAI 호출을 DB에 기록(프롬프트/응답/토큰/지연/실패 유형), 보존 기간
  후 자동 정리. Micrometer 지표(ai.calls/latency/tokens)에 무한 카디널리티
  태그 금지 원칙.
- 시세를 못 준 요청(브랜드·모델)을 로깅 — 커버리지 확대 우선순위를 감이 아니라
  실요청 빈도로 정한다.

---

## 문서 지도

| 문서 | 내용 |
| --- | --- |
| `vision-agent.md` | 3단계 프롬프트 설계, 평가 하네스, 필드별·색상 실측 상세 |
| `pricing-agent.md` | 가격 캐싱, 희소성·색상 측정 리포트 |
| `adr.md` | 결정 기록 12건 (트랜잭션 분리, 계수 실측, 희소성 미반영 등) |
| `troubleshooting.md` | 막혔던 문제들의 증상·진단·원인·해결 |
| `api-spec.md` | 프론트 연동 명세 (산정 방식·계수 출처 표 포함) |
| `ai-search-poc.md` | 임베딩 검색 PoC |
| `crawler/*/README.md` | 크롤러별 정책·한계 |
